# Social icons

Social icons (Instagram, TikTok, Facebook, WhatsApp) are currently drawn
by the shared icon system in `js/utils/icons.js` and inlined directly in
`index.html`'s footer, so no separate files are needed for the MVP.

Drop brand-approved SVG wordmarks here only if a future page needs a
standalone exported asset (e.g. a media kit page).
